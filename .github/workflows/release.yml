name: Release

# Pushing an annotated `vX.Y.Z` tag builds and signs the desktop app on every
# platform and publishes the GitHub release (which the auto-updater reads). The
# tag MUST be annotated (`git tag -a`), not lightweight — the validate job
# rejects lightweight tags. Can also be dispatched by hand to rebuild a tag.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing annotated v-prefixed tag to (re)build and publish
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate-ref:
    name: Validate immutable signed tag
    runs-on: ubuntu-24.04
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
      sha: ${{ steps.tag.outputs.sha }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false
      - id: tag
        name: Resolve and verify signed tag
        shell: bash
        env:
          REQUESTED_TAG: ${{ inputs.tag || github.ref_name }}
        run: |
          set -euo pipefail
          [[ "${REQUESTED_TAG}" =~ ^v[0-9] ]] || { echo "release tag must start with v and a digit" >&2; exit 1; }
          # checkout can materialise a tag as a lightweight ref; re-fetch the real
          # (annotated) tag object so the annotation check below is accurate.
          git fetch --force origin "refs/tags/${REQUESTED_TAG}:refs/tags/${REQUESTED_TAG}"
          git show-ref --verify --quiet "refs/tags/${REQUESTED_TAG}"
          # Require an annotated tag (not a lightweight one). GPG-signing is
          # recommended; if the tag is signed we verify it, otherwise we warn.
          if [[ "$(git cat-file -t "refs/tags/${REQUESTED_TAG}")" != tag ]]; then
            {
              echo "release tag must be annotated; lightweight tags cannot carry release metadata"
              echo "create it with: git tag -a ${REQUESTED_TAG} -m 'Vantadeck ${REQUESTED_TAG#v}'"
            } >&2
            exit 1
          fi
          git verify-tag "${REQUESTED_TAG}" || echo "::warning::Tag ${REQUESTED_TAG} is not GPG-signed (recommended for releases)."
          printf 'tag=%s\n' "${REQUESTED_TAG}" >> "${GITHUB_OUTPUT}"
          printf 'sha=%s\n' "$(git rev-list -n 1 "${REQUESTED_TAG}")" >> "${GITHUB_OUTPUT}"

  build:
    name: Build (${{ matrix.os }})
    needs: validate-ref
    timeout-minutes: 75
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            args: "--bundles nsis,msi"
          - os: macos-latest
            args: "--bundles app,dmg"
          - os: ubuntu-24.04
            args: "--bundles appimage,deb,rpm"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ needs.validate-ref.outputs.sha }}
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@67ef31d5b988238dd797d409d6f9574278e20537
        with:
          toolchain: 1.96.0
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
          cache: npm
      - name: Install Linux desktop dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - run: npm ci
      - run: npm run build
      - name: Build native bundles
        env:
          # Signs the auto-updater artifacts. Configure these repository secrets
          # from the keypair in apps/desktop/.tauri/ (see docs/release/BLOCKERS.md).
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        run: npm run tauri --workspace @vantadeck/desktop -- build ${{ matrix.args }} --config src-tauri/tauri.updater.conf.json
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: vantadeck-${{ runner.os }}
          path: target/release/bundle/**
          if-no-files-found: error
          retention-days: 14

  publish-draft:
    name: Assemble signed draft and evidence
    needs: [validate-ref, build]
    timeout-minutes: 30
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          ref: ${{ needs.validate-ref.outputs.sha }}
          path: source
          persist-credentials: false
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          path: artifacts
      - name: Generate Rust and Node source SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          path: source
          format: spdx-json
          output-file: source-sbom.spdx.json
          upload-artifact: false
      - name: Generate packaged-artifact SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0
        with:
          path: artifacts
          format: spdx-json
          output-file: artifact-sbom.spdx.json
          upload-artifact: false
      - name: Assemble distributable installers
        shell: bash
        run: |
          set -euo pipefail
          mkdir release-assets
          while IFS= read -r -d '' file; do
            case "${file}" in
              *.msi|*.exe|*.dmg|*.AppImage|*.deb|*.rpm) ;;
              *) continue ;;
            esac
            name="$(basename "${file}")"
            test ! -e "release-assets/${name}" || { echo "duplicate release asset: ${name}" >&2; exit 1; }
            cp "${file}" "release-assets/${name}"
          done < <(find artifacts -type f -print0 | sort -z)
          test -n "$(find release-assets -maxdepth 1 -type f -print -quit)" || { echo "no distributable release assets found" >&2; exit 1; }
      - name: Generate auto-updater feed (latest.json)
        shell: bash
        env:
          VALIDATED_TAG: ${{ needs.validate-ref.outputs.tag }}
        run: node source/scripts/generate-latest-json.mjs artifacts release-assets "${VALIDATED_TAG}"
      - name: Create deterministic checksum manifest
        # Runs after the updater feed step so it covers every published file,
        # including the updater .zip/.sig artifacts added above.
        shell: bash
        run: (cd release-assets && find . -maxdepth 1 -type f -printf '%f\0' | sort -z | xargs -0 sha256sum) > SHA256SUMS.txt
      - uses: sigstore/cosign-installer@398d4b0eeef1380460a10c8013a76f728fb906ac # v3
      - name: Keyless-sign artifacts and release evidence
        # Signs every published file and logs each signature to the public
        # Rekor transparency log (independently verifiable with `cosign
        # verify-blob`), without also attaching a `.sigstore.json` bundle per
        # file to the release — that data is already published, queryable,
        # and tied to this exact workflow run via GitHub's native Artifact
        # Attestations (`gh attestation verify`), so a second copy per asset
        # on the release page is redundant clutter rather than added security.
        shell: bash
        run: |
          set -euo pipefail
          mapfile -d '' files < <(find release-assets -maxdepth 1 -type f -print0 | sort -z)
          for file in "${files[@]}"; do
            cosign sign-blob --yes "${file}" > /dev/null
          done
          for file in SHA256SUMS.txt source-sbom.spdx.json artifact-sbom.spdx.json; do
            cosign sign-blob --yes "${file}" > /dev/null
          done
      - name: Attest build provenance
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32 # v4
        with:
          subject-path: release-assets/*
      - name: Attest packaged-artifact SBOM
        uses: actions/attest-sbom@c604332985a26aa8cf1bdc465b92731239ec6b9e # v4
        with:
          subject-path: release-assets/*
          sbom-path: artifact-sbom.spdx.json
      - name: Publish signed release (consumed by the auto-updater)
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3
        with:
          tag_name: ${{ needs.validate-ref.outputs.tag }}
          draft: false
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            release-assets/*
            SHA256SUMS.txt
            source-sbom.spdx.json
            artifact-sbom.spdx.json
